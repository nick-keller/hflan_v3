{% extends '::base.html.twig' %}

{% block body %}
    <h1>Blog</h1>

    {% for article in pagination %}
        <h2>{{ article.title }}</h2>
        <p>{{ article.content|stripHTML(200) }}</p>
    {% endfor %}

    {{ knp_pagination_render(pagination) }}
{% endblock %}
